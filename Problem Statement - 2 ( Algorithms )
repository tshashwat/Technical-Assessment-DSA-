arr="abbcdef"
strr=[]
def countpalindrome(arr):
    count=0
    for i in range(len(arr)):
       for j in range(i+1,len(arr)):
          if arr[i:j]==" ":
             continue
          strr.append(arr[i:j])
    for k in strr:
          if k==k[::-1]:
             count+=1
    return (count+1)
